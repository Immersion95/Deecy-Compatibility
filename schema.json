{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "array",
	"items": {
		"type": "object",
		"properties": {
			"title": {
				"type": "string",
				"description": "The title of the game."
			},
			"aka": {
				"type": "array",
				"items": {
					"type": "string"
				},
				"description": "Alternative titles (e.g., titles from other regions)."
			},
			"status": {
				"type": "string",
				"enum": [
					"Playable",
					"In Game",
					"Menu",
					"Intro",
					"Nothing",
					"Unknown"
				],
				"description": "The compatibility status. Optional: The last status_reports will be used if not specified."
			},
			"tags": {
				"type": "array",
				"items": {
					"type": "string"
				},
				"description": "Tags identifying specific features or requirements (e.g., WinCE, No VGA)."
			},
			"linked_issues": {
				"type": "array",
				"items": {
					"type": "integer"
				},
				"description": "IDs of GitHub issues related to this game."
			},
			"notes": {
				"type": "string",
				"description": "Global observations."
			},
			"status_reports": {
				"type": "array",
				"items": {
					"type": "object",
					"properties": {
						"commit": {
							"type": "string",
							"description": "The git commit hash used for testing."
						},
						"status": {
							"type": "string",
							"enum": [
								"Playable",
								"In Game",
								"Menu",
								"Intro",
								"Nothing",
								"Unknown"
							],
							"description": "The compatibility status."
						},
						"notes": {
							"type": "string",
							"description": "Observations and known issues."
						},
						"version": {
							"type": "string",
							"description": "The emulator version number (optional)."
						},
						"game_version": {
							"type": "string",
							"description": "The specific version or region of the game tested (optional)."
						}
					},
					"required": [
						"status",
						"notes"
					]
				}
			}
		},
		"required": [
			"title",
			"tags",
			"status_reports"
		]
	}
}